# R3-2 ④ 专家团评审

- 架构师（Winston）：**同意迁移**。条件：用**独立 `DSH_HOME` 副本**验证；理由要摆正 —— 不是救插件，**被甩下的是树自己**（插件已跑到 `^0.1.5-rc.1` 线）。
- 业务分析（Mary）：**有保留**。单向门代价确定、收益无清单 ⇒ 要求先给"迁移后免费 / 仍需自建"清单。
- 产品经理（John）：**同意迁移**，但**反对与动作 MVP 捆绑**。

分歧（未抹平）：迁移时机（架构师现在 / 分析师等清单 / 产品经理反对当 MVP 前置）；迁移源谱系未证实。

架构师另提两条开工前必答项：① **新线"打开" v2 会话是否会写出 v3 后继文件**（若会，"只读验证"不成立，副本必须整个 `DSH_HOME`）；② 动作的**主体**与权限模型（`kill(id, caller)` 要求 caller 是 job owner，用户点击没有独立 principal）—— 后者属 `r3-observe-to-act` 的前置。
