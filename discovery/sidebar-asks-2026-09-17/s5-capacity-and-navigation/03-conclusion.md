# S5 右侧栏的容量与导航 ③ 结论与建议

- 治理原则：默认隐藏 + 溢出入口 + 数量预算，而不是禁止新增类型。
- 因无 order/icon 字段，"默认可见预算"与"降到图标轨道"今天无法表达 ⇒ 元数据先行是执行顺序上的硬前提（决策 5）。
- 准入判据采纳：新增 kind 需拥有独立 dsh-resource:// 地址域。
- 与整栏第三方插件的关系当前无需处理（已禁用），保留"声明式表面所有者 + 冲突检测"作为后续方向。
