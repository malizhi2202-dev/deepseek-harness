# R2-4 ④ 专家团评审

- 产品经理（John）：**同意只读那一半**，但排序与架构师相反：先验证有人真的看这个面板。
- 架构师（Winston）：**有保留，且明确反对其中一半** —— 只读镜像可以；把 `pty`/`terminal-bash` 挂进 web profile preset **不行**（在无认证层下把"有审批有日志"换成"绕过模型无日志"的通道）。条件：新增只读 RPC + 服务端映射 owner + 绝不接受客户端断言 owner，必须作为不可配置安全不变量**先落地**。
- 业务分析（Mary）：**有保留**。威胁模型地基（"能连端口＋伪造可信 Host 就已拥有 GUI"）未写成文档，且全部取决于部署绑定面（仅 loopback 还是可达 LAN）—— 这一个未知可推翻其余全部。

分歧（未抹平）：只读终端是"产品"还是"妥协"；先验证使用还是先拆决策；三人在此无交集。**本轮唯一出现真反对的子议题，不得当作已达成共识。**
