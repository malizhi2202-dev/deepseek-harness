# R2-5 ⑤ 人工审核记录

- 审核时间：2026-09-17（第 2 轮审核门）
- 审核原话：「ok」（对 ⑤ 门推荐版本的通过）
- 结果：**通过**。结论落盘为"对齐版本线而非修插件"；三条路 A/B/C 记录在案。
- **P2 已获答（2026-09-17）：先做有界代价评估再定** —— 负责人不直接迁移、也不直接自建，要求先把"迁移到受支持线（如 `0.1.5-rc.1`）的代价"量出来。该评估成为第 3 轮的子议题之一（`r3-version-line-cost/`）；评估结果出来前不启动 A 或 B 的实现。
- 本流程不执行代码改动。

## 后续订正（2026-09-17，由 `discovery/plugin-line-compatibility/` 的扫描带回）

对 `02-competitive-recon.md` 第 3 条与 `03-conclusion.md` 的订正：原文写「该插件自 0.1.13 起不再声明任何 DSH peer 依赖 ⇒ **最新版兼容性无法从元数据判断，只能源码核查**」。**该结论只对 `peerDependencies` 成立**。经 registry 元数据实测，该插件把目标线声明移到了清单字段里，最新版明确声明 `dsh.engines.dsh = ">=0.1.5-rc.1"`。

- **元数据能判**，且判定结果是：最新版**要求受支持线**，与当时检出线明确不匹配 ⇒ 「源码核查」这一步的缺口**不必要**，本议题的结论（对齐版本线而非修插件）**由该元数据进一步确认**。
- 连带订正：本议题所依赖的清单类型包是**共享词表**（每个读者自负校验），且该目标线字段**至今不在词表成员内、全仓无人读取**；本节只记录事实订正，**不改变本议题已过门的 A/B/C 路线与 P2 裁决**。
- 该字段的归属与语义（是否进词表、只写下限还是可写上界）属**词表所有者**裁决，已登记在 `../../plugin-line-compatibility/99-implementation-checklist.md` 的 H2。
