# S1 任务观测的形态与信息架构 ③ 结论与建议

- 任务观测 V1 合理且已交付；jobs 段与会话头部入口重复，侧栏应只留汇总 + 失败，实时输出与强杀留给头部与生态。
- 不做并列多 tab；列表/树/时间线作为同一面板内的镜头。
- 级联只画两类可证边，第三类默认不画并显式标注"无任务依赖模型"。
- 采纳验收口径：任意一次失败 ≤2 次点击内看到它属于哪一类；并有动作闭环（跳 seq / 换模型重试 / 派修复子代理）。
