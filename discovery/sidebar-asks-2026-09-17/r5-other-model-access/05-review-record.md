# R5-5 ⑤ 人工审核门记录：其他模型接入

五步流状态：① 完成 ② 完成 ③ 完成 ④ 完成 ⑤ **通过（拆分为两件独立议题）**。

## 一、门内裁决（原文）

| # | 裁决原话 | 含义 |
| --- | --- | --- |
| 1 | 「1和2同意」 | **接受拆分**：甲「协议错配」（R1–R3）**立刻做**；乙「CLI 路由」（R4／R5／R6）**只留决定记录、不关闭**。并同意甲的执行范围：**R2 与 R1 同批**、**R3 单独一条** |
| 2 | 「3 包内 owner吧」 | `api` 的**权威归属**定为**包内 owner**——即在 `packages/llm/llm-pi-ai/src/provider.ts:46-51` 的 `PROTOCOLS` 与 `src/discovery.ts:39-43` 的 `LISTABLE_PROTOCOLS` 之间指定唯一真相来源，**不是** UI owner |

## 二、三处缺口的处置

| 缺口 | 处置 | 依据 |
| --- | --- | --- |
| 无主的已知缺陷（注释承认「鉴权类失败会被读成凭据问题」而风险清单未收录） | **记为已知盲区**，在本档第四节留痕，并作为 R2 的实施动机 | `packages/llm/llm-pi-ai/src/discovery.ts:293-298`；`../05-risks-and-gates.md:33` |
| 无验收形态（未声明协议时用户看到什么） | **解除，且零客户端改动**：卡片的协议状态初值就是可用协议表的第一项（`CustomProviderCard.tsx:84` 的 `useState(protocols[0] ?? '')`），因此**设置页永远点名协议**，根本到不了这条拒绝；该拒绝只对非 UI 调用方可达，无需新设计验收形态 | `packages/client/ui-settings-models/src/client/CustomProviderCard.tsx:143`、`:271`；`ModelListEditor.tsx:58`、`:236`；`packages/llm/llm-pi-ai/src/config.ts:321`；`catalog.ts:878-882` |
| 无回归证据指定 | **实施时待补**：归因文案与协议判别须各有一条断言；专家团明言**未核实发现路径自身的测试面**，故这是待办而非结论 | 见第五节 |

## 三、本轮折入的订正（六处）

1. **R4 的理由换掉**：原「日志记录了并未真正执行的调用」**不准确**；调用确实执行了，**假的是日志对这次执行的描述**，且不变量（`invariant.ts:31-52`）**全是日志内部自洽性检查**、对此结构性失明。主 agent 已逐字复核 `:39-42`（`session.deriveMessages()` 比对出站请求与日志推导）与 `:44-52`（`headerMatches` 比对 `options.*` 与折叠 header）。
2. **R6 的断言降级**：「0 个方向需要改会话格式」→「**本议题范围内未发现**」，并写明证据边界（四条依据只覆盖「模型调用」这一种执行体）。
3. **R4 的记录形态**：写成「**边界未定，暂不做**」（可翻转），不写成永久性技术否定。
4. **两张协议表的表述精确化**：取值**今天一致**，缺的是**派生关系声明**。
5. **四面行号/措辞精度订正**：事件词汇表位置、测试替身注册点、`catalog.ts:871-877` 的 `invalid()` 旁证、以及 `cli-mock-llm.ts` 的**防误读**（它是冒烟替身，不是 CLI-as-route 的先例）。
6. **承诺行的引用位置**：「model-visible ⟺ logged」应引 `packages/llm/llm/README.md:108`（权威行），`:12` 是同页长摘要。

## 四、已知盲区（本轮明确记录、不在本议题内修）

1. **归因契约天然不覆盖非 HTTP**：契约句只管 HTTP（`packages/llm/llm/src/index.ts:193-195`），归因实载只有一个 `user-agent` 头（`attribution.ts:67`），且 `scripts/` 下**无覆盖该契约的门禁**（N20：4 处 attribution 命中全属许可证归属、工具目录条目归属、清单路径等其他语义）。⇒ 非 HTTP provider 天然在覆盖之外。本议题「不做 CLI 路由」以**偶然**方式保护了这段覆盖率——**不是设计，是巧合**。
2. **风险条目的「降级」措辞读起来像完成态**：`../05-risks-and-gates.md:33` 的对策含「给出可执行错误与**降级**」，但仓库里**没有任何降级机制**（`discovery.ts:300-305` 是拒绝 + 手工录入，`catalog.ts:878-882` 是报错）。风险条目被读成已实现能力，是审计上最危险的一类信号。
3. **无主的已知缺陷**（第二节第一行）。

## 五、下一步（本档落盘后立即执行）

1. **甲（实施）**：R2 + R1 同批、R3 单独，全部落在 `packages/llm/llm-pi-ai`；并指定 `api` 的包内唯一真相来源；实施时补第三节缺口所需的两条断言。
2. **乙（记录）**：`../r6-cli-route-decision/decision-record.md`，保留 R4 的护栏作用与 R5 的否决，附修正版理由与**复活条件**。

## 六、落盘形态

- 甲与乙**同属一个五步流程**（五步是对合并议题跑的），故五步记录落在 `r5-other-model-access/`（01–05），乙作为**派生决定记录**单独归档于 `../r6-cli-route-decision/`，并互相单向引用。
- 未改动任何既有议题目录（`../r2-*`、`../r3-*`、`../r4-*`）。

## 七、事实分级统计

| 分级 | 条数 | 说明 |
| --- | --- | --- |
| 【复核】 | 本议题全部仓库事实 | 主 agent 或子代理读代码确认，均带 `文件:行号`；四处行号精度已订正 |
| 【联网·子代理·未自核】 | 14 个来源主体、21 条 URL、50 条引文 | **主 agent 未自核**（唯一一次直连被截断）；仅 1 条承重主张有双源印证；含 1 条已撤回预设。**不得作为门禁条款** |
| 【未验证】 | 见 `02-competitive-recon.md` 第七节 | 严格区分「未找到来源」与「已确认缺席」 |

## 八、合规自检（命令实跑）

- 反向检索网络监听端点、主 agent 家目录绝对路径、远端账号名、提交短哈希、进程标识、部署侧实施用语：**命中均为 0**。本句刻意**不复写**这些字面量，以免自检句自身成为命中。
- 每个文件末尾恰好一个换行；文档一段一个物理行。
- **未改动任何代码**（本档落盘时）：本议题的代码变更在下一步执行，不在五步流程内擅动。
- 未触碰被明令排除的临时目录。
