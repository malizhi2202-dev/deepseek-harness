# S6 智能体派生面板 ③ 结论与建议

- 形态：独立 agents kind 的新包（第四个 kind），不是"任务观测"内的镜头，也不是改名。
- 不与浮层重复的关键差异：用全量会话摘要（useSessions(state => state.byId)）建树，完整且可回溯；subagentsByParent 是懒的。
- 复用策略：不复用组件（其 props 绑在头部 slot 契约上）、不跨包运行时导入（packages/client/AGENTS.md 导出规则 3）、算法回到真实属主导出。
- 头部浮层保留并增加"在面板中打开"入口。
- 明确否决：任务依赖图、客户端跨会话翻日志做失败归因、只画活子代理、力导向图画布。
- 承重论证需对抗性检验："只有 tab 类型能拥有 guide 条目 ⇒ 必须独立成 kind"。
