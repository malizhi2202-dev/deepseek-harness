# R3-1 ④ 专家团评审

**专家团本轮新核对的三条事实**：
1. `session.command(line)` 只回 `{ matched: boolean }`（`contract/session.ts:140`）⇒ 分不清三态（主 agent 已复核原文）。
2. 生态中发布的 `dsh-checkpoint-rewind 0.5.2` 把 dsh-llm/dsh-session/dsh-storage-domain/dsh-tools/dsh-typert-protocol **全部精确钉在 `0.1.0-rc.6`**（主 agent 已复核）。
3. **交叉结论**：MVP 头号动作（回滚＋两段式预览）整个建在这个声明上已不兼容的插件上，而 0.6.9+ 才接受 0.1.5 线 ⇒ 动作 MVP 与迁移决策被同一个插件焊死。

逐条表态：
- 产品经理（John）：① 同意；② **反对**"先跳转"（回滚是唯一自带可逆保证的动作，只上跳转是观光不是闭环）；③ 反对把停 job/派子代理放进本轮 MVP。
- 架构师（Winston）：① **有保留**，规则收窄为"改状态走命令、移视口走类型化调用"；② 改推**先跳转后回滚**（风险顺序 + 跳转是回滚前置）。
- 业务分析（Mary）：① **有保留**（它是便宜不是对：用户没输入过的命令出现在界面里）；② 同意选这两项、**顺序弃权**（无用户证据）；③ 指出停 job 与派子代理**不对等**（后者依赖失败上下文包）。

分歧清单（未抹平）：MVP 顺序（PM vs 架构师 vs 分析师弃权）；命令行的适用边界（PM vs 架构师）；本轮是否碰停 job/派子代理（三人三说）；`unattributed` 时按钮硬禁用还是仅警告（分析师 vs 产品经理）；迁移时机（见 `r3-version-line-cost/04`）。

架构师的开工前必答项（未决，登记为独立议题的前置）：**动作的主体是谁？** `kill(id, caller)` 要求 caller 是 job owner，而"用户点击"没有独立 principal ⇒ 权限模型必须先定义。
