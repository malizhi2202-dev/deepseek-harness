# 插件目标线兼容：⑤ 人工审核门记录

五步流状态：① 完成 ② 完成 ③ 完成 ④ 完成 ⑤ **通过（议题判为溶解，仅移交可落地部分）**。

## 一、落盘形态

独立议题目录（体例照 `../failure-attribution/` 的八文件）。理由：本议题与右栏议题无关，由只读扫描独立发现，不属任何既有议题的下游。

## 二、门内裁决（原文）

| # | 裁决原话 | 含义 |
| --- | --- | --- |
| 1 | 「1」 | 采纳 ③ 的第一问：**本议题就此关闭，判为溶解** |
| 2 | 「并且把能落地的先落地」 | 要求把**能落地的部分立即落地**，不等后续裁决 |

## 三、三问的追加裁决

第三轮问答时抛出、当场未获答复的三条；门内随后追加裁决「**依你的建议**」（2026-09-17），即采纳 `04-conclusion.md` 第六节与 `99-implementation-checklist.md` 给出的三项建议：

1. 幸存缺口「挂起成因」→ **并入既有插件设置页议题**，不在本议题内立项（H1）。
2. R2 与 R5 的否决 → **接受并记录**，连理由与复活条件一并冻结（X1、X2）。
3. 目标线字段 → **以移交便条形式记录，不主动推进**（H2）。

## 四、本轮折入的五处订正

1. **「权威 schema」表述作废**：清单类型包自述「每个读者自负校验」（F3）⇒ 改称**共享词表**。
2. **「`dsh.client.inject` 无人读取」表述作废**：它被读取（解析、投影进依赖、归一化），只是不排序激活（F4）。
3. **「失败静默」表述不完整**：挂起状态在既有设置页可见（F12）；真正不可见的是**成因**（F13、F14）。
4. **专家团 R1 的前提被既有能力取代**：R1 不是待做项（F12）。
5. **专家团「R4 落点不存在」的理由更正**：本仓有插件状态面，但它被设计为只读、无变更路径（F19）。

## 五、事实分级统计

| 分级 | 条数 | 说明 |
| --- | --- | --- |
| 【复核】 | 19 | 由主 agent 直接跑命令或读文件确认（含 registry 元数据实测与 semver 实测） |
| 【联网】 | 0（本档） | 外部事实集中在 `03-competitive-recon.md`，全部标【联网】且全部未经主 agent 独立复核 |
| 【未验证】 | 0 | 未能确认者不以事实形式出现，统一进 03 的未验证清单 |

`01-verified-facts.md` 共 19 条事实；`03-competitive-recon.md` 含 16 行对照表、16 条逐字引文、4 条撤回声明。

## 六、编号纪律

- 事实 `F1–F19`（`01`）、方向 `D1–D20`（`02`）、外部对象与引文 `E1–E16` 及表格行号（`03`）、建议 `R1–R5`（`03`/`04`）、分歧 `G1–G7`（`05`）。
- 跨文件引用一律用编号；行号只出现在 `01-verified-facts.md` 的事实出处处与 `02`/`05` 的证据补充处。

## 七、合规自检（命令实跑）

- 反向检索网络监听端点、主 agent 家目录绝对路径、远端账号名、提交短哈希、进程标识、部署侧实施用语：**命中均为 0**。本句刻意**不复写**这些字面量，以免自检句自身成为命中（此为上一轮踩过的坑）。
- 每个文件末尾恰好一个换行；文档一段一个物理行。
- 未改动任何代码；未改动任何既有议题目录；未触碰被明令排除的临时目录。

## 八、本轮明确未做

- **未改任何代码**（本议题判为溶解，无可实施项落在本议题内）。
- **未引入版本校验、未引入上限字段、未改动挂起语义、未改动会话格式**。
- **未提请词表所有者**（属未裁决项）。
- **未在既有议题目录内追加文件**（避免形成第二个权威）。

## 九、为什么不做「先加个版本检查试试」

三条独立理由，任一条单独成立即可否决：

1. **第一个判决就是错的**：朴素的版本判定会先把**本仓当前检出线自己**判为不兼容（F15 第一组、F16）。
2. **会罚到守规矩的一方**：目标线声明的漂移方向决定，此刻加门会挡住仍写旧通道的老插件、放过漂移到无人执行通道的新插件（F17、F18）。
3. **没有落点**：加载期未必读得到清单（F8、F9），安装期无市场实体（F19）。
