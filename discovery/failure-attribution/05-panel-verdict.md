# ④ 专家团评审

方法：`bmad-party-mode`（由只读子代理执行）。**流程偏离声明**：该技能的名册解析依赖的本机条件不满足，按指示落回**三视角** —— John（产品经理 / 用户价值）、Winston（架构师 / 长期代价）、Mary（业务分析 / 仓库惯例）。全程只读、未写盘。

## 总判词

**同意 1 · 有保留 2 · 反对 0。** 无人反对整体方向；分歧全在**切片范围与排序**。

## 三视角

**John（产品经理）—— 同意**

- R1a+R1b 是唯一今天能兑现用户价值的部分；F7 是**已在线**的错误归属（用户现在就可能在读错的步号）。
- 砍：R1a 里的 `authority` 四态（用户看不见，那是内部报表语言；用户要的是「要不要重试」）。
- 危险动作：若把 `null`（未 conclude）那格判成失败，用户点「重试这一步」会重复一次**本已成功**的模型调用，工具带副作用时即重复副作用；而 F7 使任何挂在「失败步」上的按钮**今天就已经在指错步**。
- 翻转条件：若产品不做步级重试动作，R1a 的产品价值塌一半，价值归属挪给 R3。

**Winston（架构师）—— 有保留**

- 可行性认（F3 的次序可核）。
- 砍：把 R1b 当**独立切片** —— 要做对必须回答「哪个步失败了」，而那正是 R1a 的谓词；不共用就会把逐字相同的 `lastStep` 再写一遍，R3 落地时改第三遍。
- 硬前置：① 纯投影**无法干净识别合成事件**（N6）—— 要么承认检疫是结构性推断并写清，要么承认它也是一种缺字段；② `authority` 归属必须二选一。
- 危险动作：`authority` 四态若被 UI 用来归咎「用户」，会让用户反复改写 prompt、掩盖真实故障；R1b 修对位置后若同时开放「重跑该步」，危险动作会**更精准地打在一个已成功执行过的步上**。
- 翻转条件：F4 若同批解决，R3 必须提前且先裁决「工具失败是否算步失败」；`null` 分支若被取消，谓词第三态可删；冻结清单规则若改为「新增可选成员不使冻结描述失效」，N4 需重开。

**Mary（业务分析）—— 有保留**

- 否决 V1–V4 在事实层全站得住；保留的是 R1a 的「规范先行」倾向。
- 砍：R1a 的 `unattributed` 三问标准（无已承诺消费者就先立标准 = 先写没人读的规范；等接上消费者，标准会从实现里自然长出）。
- 注意：F8 的格式代价**必须按类型分账**（见 N3/N4/N5）。
- 危险动作：只发投影不发 R1b ＝ **明知 F7 在骗用户却先建更漂亮的新管道**；风险不是新增而是**被延长**。
- 翻转条件：若 F5 的合成路径补上统一「合成」标记，Mary 的保留降为同意。

## 分歧清单（不抹平）

| # | 主张方 | 内容 |
| --- | --- | --- |
| G1 | John + Mary | R1b 应**立刻独立发出**（今天的用户可见错误一天都不该多等） |
| G2 | Winston | R1b **不得独立发出**，必须紧跟 R1a 共用同一谓词（否则同一处 UI 改两遍，第一遍必然写错） |
| G3 | 三人一致 | `authority` 四态**砍出 R1a**、并入 R3（持久字段才有唯一 home）——**门内已裁决采纳** |
| G4 | Winston（自提反证） | `authority` 今天在 R1a 里**就能推导**（N2）；他不据此主张保留，而据此主张更该砍（推导会制造第二真源） |
| G5 | Mary | `unattributed` 三问标准应砍，等消费者出现再从实现里长出 |
| G6 | John + Winston | 检疫 + `unattributed` 是诚实底线，砍了 R1a 价值只剩一半（与 G5 正面对撞） |
| G7 | Winston | 检疫在纯投影里做不干净（N6），反对把 R1a 说成「零代价」 |

## 三视角一致同意的部分

1. 维持 V1：步级「没正常收尾」**确实可判定**，**不需要新增事件类型**。
2. 维持 V2：源侧终点是三态（N1）+ 两层（F4）+ 合成路径（F5），布尔必撒谎。
3. 维持 V3：`turn/end` 加 step 坐标在粘滞折叠下**坐标不唯一**。
4. V4 与 R3 不矛盾：今天不加、排进队列，二者一致。
5. **可判定 ≠ 可归因**：F4 + F5 + F9 合流，归因必有一部分落 `unattributed`。
6. R1b 不许再写一遍位置推断：两处函数体逐字相同（N7），必须一份共享实现。

## 最终排序建议（三方交汇解）

1. **R1a（先做）**：谓词四格 + 解失败原因 + 合成事件检疫 + `unattributed`；**砍掉 `authority` 四态**。检疫必须显式承认为「结构性缺席推断」（N6），不得称零代价。
2. **R1b（紧随，不拖）**：用 R1a 的谓词替换两处 `steps.at(-1)`，抽共享助手。**这是 G1 与 G2 的交汇解**：John/Mary 要「今天就别骗用户」、Winston 要「别改两遍同一处」，二者兼容的**唯一前提**是 R1b 建立在 R1a 谓词上 ⇒ R1b 应标为「**依赖 R1a**」，不得当独立小改。
3. **R3（保持排队）**：`step/end` 可选 `outcome`，`authority` 随它走；代价见 N3/N4、三条 close 路径一致（`repair` 合成**原理上无法知道结局**，必须定义它写什么）；生成物不受影响；旧构建不拒读。**附加前置**：R3 一旦启动，必须先裁决「工具失败是否算步失败」，否则 F4 会让 `outcome` 变成第二个语义不确定的字段。

## 门禁判断（专家团原判）

**议题本身已收敛，但按现状不建议直接过门**，缺两条（均为文档/裁决级，不需新代码事实）：① R1a 的消费者 `session-outcomes` 不存在（N9），缺验收标准；② `authority` 归属未裁决。

⇒ 负责人在 ⑤ 门内**同时裁掉了第②条**（随 R3 走）并批准落盘，第①条作为**门内待裁决**写明（见 `06-review-record.md`）。
