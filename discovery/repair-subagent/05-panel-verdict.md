# 05 · 专家团评审（三视角）

组织方式：按 `bmad-party-mode` 应解析 BMAD 名册；本仓库根无对应项目配置，且解析脚本需写盘、与只读约束冲突，故按约定的**三视角**退路执行：John（产品 / 用户价值）、Winston（架构 / 长期代价与安全）、Mary（业务分析 / 仓库惯例与审计）。开工前已只读复核 01 的关键 `文件:行号`。

**总判词：同意 1 · 有保留 2 · 反对 0。可进「范围／议题」门，不可进「实现」门。**

## 一、三视角立场

| 视角 | 立场 | 一句话理由 | 最该砍的一处 | 开工前必答 |
| --- | --- | --- | --- | --- |
| John | 同意 | 失败后用户只有「重试 / 放弃」，只读修复者是唯一不赌副作用就能把失败换成信息的动作 | R4（载荷本就是自由文本，失败上下文包可整包进 prompt） | 人类入口不做出来，用户不会用 |
| Winston | 有保留 | 「需要新机制」这个前提可再削：现有委派工具 + 结构化 brief 今天**零代码**就能派一个只读修复者 | R1 里「需要新机制」这一层 | ①「只读」由谁执行；② 派生主体谁解析 |
| Mary | 有保留 | 唯一一条会把 R1 从「零格式变更」改判为「格式级」的证据在这里 | 不是砍 R4，而是**按住**它 | 失败上下文包的**事件化**结论 |

## 二、分歧清单（不抹平）

| 编号 | 分歧 | 主张 |
| --- | --- | --- |
| G1 | R1 的实现层级 | Winston：先降到「现有委派 + 结构化 brief」零代码验证一轮；John 反对（无一等入口用户不会用）；Mary 记录代价（若 Winston 对，交付面缩到提示词模板，R2/R3 全后置） |
| G2 | 「只读」的地位 | Winston：未执行的承诺，必须落工具守卫；John：产品取舍而非安全问题；Mary：不可审计则结论写不进报告 |
| G3 | R2 的审计落点 | Winston：先收口上游未决项；Mary：先答事件化；John：同意 R2 但反对因此拖住 R1 |
| G4 | R4 的用词 | John 与 Winston 用「砍」；Mary 用「按住」 |

## 三、三视角一致同意的部分

1. **V2 成立**，且是安全结论：命令路径上的审批是假审批。
2. **V5 成立**：无先例，且 Airflow 明确警告 context 只反映部分状态。
3. **V3 成立**：不允许既有委派工具自我递归。
4. **写类能力必须在人工确认之后**：审批结果含 fail-closed 的不可用态，且守卫单调不可翻回。
5. **人类入口手势不可伪造** ⇒ **R2 方向三票通过**。
6. 新路径必须带显式次数与预算，并承认冷恢复不继承预算。
7. **F8 模板可照抄，但须写明边界**：它是冷加载合成的工具结果，把它当通用归因模板属**扩大适用面**。
8. **R4 今天不动。**

## 四、最终排序建议

`R1 → R3a（随 R1 一次带上显式次数与预算）→ R2（人类手势入口 + 宿主解析 + 审计）→ R3b（按父级并发上限、写类人工确认、发起者不得自批）→ R4 等第二消费者`

保留异议：John 主张 R2 不应排在 R3a 之后；Winston 主张 R1 与 R3a 都可先不做，零代码试一轮。

## 五、Mary 的新增硬发现（唯一改判风险）

`AGENTS.md:108` 与 `AGENTS.md:111` 合起来意味着：**R1 的失败上下文包可能才是本议题真正的格式级决定**，而 F7b 只覆盖描述符、**不覆盖会话事件**。若上下文包要成为父模型可见的输入，就必须落成 `user/message` / `assistant/message` / `tool/result` 之一（F12a），否则违反「Model-visible ⟺ logged」。

按 F14/F15 的具体形态，这条可以判定为：**模型侧派生今天已经是 surface 事件**；**人类侧派生写的是子日志、父会话零事件**。于是真正待答的是一个二选一：人类触发的派生要不要让父模型看见。

## 六、门内裁决（人工审核门给出）

1. 修复者首发默认刻度为**只读**；需要写时**必须人类同意**。
2. **派生动作本身是危险动作**，因此必须人类确认，且**不能走命令路径**。
3. 上游未决项**不先收口**，不作为本议题前置。

## 七、门后仍开放的三项

| 编号 | 开放项 | 现状 |
| --- | --- | --- |
| O1 | 「只读」如何强制 | 载体已存在（可续跑描述符带 `toolFilter`），缺「禁哪些工具、谁来配」；一次性描述符不带该字段，属已知不对称 |
| O2 | 人类触发的派生是否需要父模型可见 | 未答；答「需要」即引入 surface 事件，R2 升为格式级 |
| O3 | 宿主侧主体解析的设计 | 门内裁决 2 已排除命令路径，方向明确，细节未定 |
